import { StyleSheet } from 'react-native';
import { COLORS } from './../constants/';

export default StyleSheet.create({
  screenTitle: {
    height: 40,
    width: 150,
    marginLeft: 20,
    marginRight: 20,
    resizeMode: 'contain',
  },
});
